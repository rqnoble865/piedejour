{
  "name": "piedejour",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal local-first desktop journal.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check app.js"
  },
  "devDependencies": {
    "electron": "^42.5.0"
  }
}
